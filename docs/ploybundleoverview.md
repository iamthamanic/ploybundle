# Ploybundle overview (redirect)

The canonical overview for humans and agents lives at the **repository root**:

**[../ploybundleoverview.md](../ploybundleoverview.md)**

That file is intentionally at the root so it stays easy to find, commit, and push. Deep-dive docs remain under `docs/` (architecture, CLI, presets, security, …).
